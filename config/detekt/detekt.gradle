apply plugin: "io.gitlab.arturbosch.detekt"

project.afterEvaluate {
    tasks.named("preBuild") {
        dependsOn("detekt")
    }
}

detekt {
    toolVersion = "1.23.1"
    def inputDirFiles = []
    rootProject.subprojects.each { module ->
        inputDirFiles << "$module.projectDir/src/main/java"
    } //dessa forma consigo usar detekt em todos os módulos
    source = files(inputDirFiles)
    config.setFrom(file("$rootDir/config/detekt/detekt.yml"))
    autoCorrect = true


    reports {
        xml.required.set(true)
        html.required.set(true)
        txt.required.set(true)
        sarif.required.set(true)
        md.required.set(true)
    }
}
//caso nao encontre a task detektGenerateConfig, rode ./gradlew detektGenerateConfig no terminal
//caso nao encontre a task detekt, rode ./gradle detekt no terminal